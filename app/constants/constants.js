export const constants = {
    events: {
        startSelector: ".start",
        pauseSelector: ".pause",
        stopSelector: ".stop",
        worldSelector: "#select",
        worldContainer: ".world",
        sizeSelector: "#size"
    },
    timeInterval: 500,
    defaultSize: 100
};