export const constants = {
    events: {
        startSelector: ".start",
        pauseSelector: ".pause",
        stopSelector: ".stop",
        worldSelector: ".container",
        sizeSelector: "#size"
    },
    timeInterval: 500,
    defaultSize: 100
};